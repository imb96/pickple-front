name: Sync and merge upstream repository
on:
  workflow_dispatch:
  schedule: 
  - cron: "*/1 * * * *"

jobs:
  merge:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Merge upstream
        run: |
          git config --global user.name 'imb96'
          git config --global user.email 'kimminje7267@naver.com'
          # "git checkout master" is unnecessary, already here by default
          git pull --unshallow  # this option is very important, you would get
                                # complains about unrelated histories without it.
                                # (but actions/checkout@v2 can also be instructed
                                # to fetch all git depth right from the start)
          git remote add upstream https://github.com/Java-and-Script/pickple-front
          git fetch upstream
          git checkout dev
          git merge -Xtheirs upstream/dev
          git push origin dev
          # etc
